# Google Maps JavaScript API key (used by the browser to render the map + Places).
# Restrict this key by HTTP referrer to your GitHub Pages domain, and set quotas.
# Without it, the app still works on bundled static hotel data and the list view.
VITE_GOOGLE_MAPS_API_KEY=
